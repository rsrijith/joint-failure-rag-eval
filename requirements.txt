# Core
python-dotenv>=1.0
pydantic>=2.5
requests>=2.30

# Data loading
datasets>=3.0
huggingface_hub>=0.27

# Perturbation generation + Claude judge
anthropic>=0.40

# Judges — proprietary / open-via-API
cerebras_cloud_sdk>=1.30
mistralai>=1.5

# Judges — local NLI models (HHEM, MiniCheck, AlignScore)
# Pinned to transformers 4.x because HHEM-2.1-Open's custom class
# targets the 4.x API (uses _tied_weights_keys, not all_tied_weights_keys).
transformers>=4.46,<5.0
torch>=2.0

# Judges — local models (added when implemented)
# transformers, sentence-transformers, torch — added in later commit
# alignscore — added in later commit

# Utilities
numpy>=2.0
pandas>=2.2
tqdm>=4.66
